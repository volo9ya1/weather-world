pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp style.css public/
    - cp script.js public/
  artifacts:
    paths:
      - public
  only:
    - main
    - master
